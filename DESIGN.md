# taxback365 — DESIGN.md

> 이 문서는 **현 코드의 거울**이다. 값의 1차 출처는 `app/globals.css` 의 `:root` 이고,
> 여기 적힌 수치가 그것과 어긋나면 이 문서가 틀린 것이다.
>
> 대비값은 전부 `scripts/contrast-check.mjs` 로 WCAG 실측했다. 짐작을 적지 않는다.
>
> ⚠️ 이 파일은 Tailwind 소스 스캔에서 제외돼 있다(`globals.css` 의 `@source not`).
> 이전 판본이 존재하지 않는 토큰(`--color-accent-mint` 등)을 예시로 적어 두었고,
> Tailwind 가 그것을 실제 유틸리티로 생성해 **선언 없는 변수를 참조하는 무효 CSS** 를
> 만들고 있었다. 문서의 예시가 빌드 산출물을 오염시켜서는 안 된다.

---

## §1 Visual Theme — Swiss Grid · Document as Layout

**한 줄로**: 페이지를 화면이 아니라 **한 장의 서류**로 조판한다.

연말정산은 원래 서류 작업이다. 원천징수영수증에는 항목 번호가 있고, 라벨 열과 금액
열이 있고, 합계 행이 있다. 이 제품의 콘텐츠가 이미 그 구조이므로 조판을 억지로
입히는 것이 아니라 **원래 형태로 돌려놓는 것**이다.

세 원리:

1. **면이 아니라 선이 구획한다.** 카드(`둥근 모서리 + 회색 면 + 테두리`)를 기본
   단위로 쓰지 않는다. 규칙선 3단이 위계를 만든다.
2. **절(節) 단위로 나눈다.** No.01~05. 좌측 번호 열이 페이지 전체를 관통하는 축이다.
3. **숫자가 조판의 축이다.** 금액은 전부 mono + tabular-nums + 우측 정렬. 라벨/값/상태
   열의 정렬이 페이지 전체에서 일관되어야 서류로 읽힌다.

**금지**: 그라데이션, 드롭섀도우, 둥근 카드, 분위기용 글로우, 이모지.

---

## §2 Color Palette & Roles

`app/globals.css` `:root` 와 1:1 대응한다. 괄호 안은 흰 지면 대비 실측값.

| 토큰 | 값 | 대비 | 역할 |
|---|---|---|---|
| `base` | `#ffffff` | — | 지면 |
| `surface` | `#ededed` | 1.17 | 얕은 면 (판권면 등) |
| `surface-2` | `#e4e4e4` | 1.27 | 표 합계 행 강조 |
| `surface-3` | `#dbdbdb` | 1.38 | 3단계 면 (희소) |
| `edge` | `#d4d4d4` | 1.48 | **행 구분 괘선 — 장식선** |
| `edge-strong` | `#949494` | 3.03 | **폼 컨트롤 경계 — WCAG 1.4.11** |
| `hi` | `#0a0a0a` | 19.80 | 본문 · 구획선 · 문서 경계선 |
| `mid` | `#5f5f5f` | 6.39 | 보조 텍스트 |
| `dim` | `#767676` | 4.54 | 3차 텍스트 (AA 하한) |
| `accent` | `#d92d33` | 4.80 | CTA 면 · 상태 표식 · 포커스 링 |
| `accent-ink` | `#c0242a` | 5.95 | **소형 accent 텍스트 전용** |
| `ink` / `on-accent` | `#ffffff` | 4.80 (on accent) | accent 면 위 텍스트 |
| `rose` | `#8c1d13` | 9.13 | 오류 |
| `amber` | `#8a5a00` | 5.93 | 주의 |
| `sky` | `#0b57d0` | 6.39 | 정보 |
| `scrim` | `rgba(10,10,10,.5)` | — | **모달 백드롭** |

### 경계선 두 종류를 혼동하지 말 것

WCAG 1.4.11 의 3:1 은 **사용자가 조작하는 컴포넌트**의 경계에만 걸린다.

- 입력·버튼·선택 컨트롤 → `edge-strong`. 흰 지면에서 `edge` 로는 **조작 가능한
  요소가 보이지 않는다.**
- 표 행 구분, 구획선 → `edge`. 장식 괘선에 3:1 을 강요하면 조판이 시끄러워진다.

### accent 를 어디에 쓰나

신호적색은 **작지만 강한 면**이다. "작고 약한 글씨"로 흘리지 않는다.

- 쓴다: 상태 표식, CTA 면, 슬라이더 지표, 브랜드 마크의 짧은 막대, 포커스 링
- 소형 텍스트에 부득이 필요하면 `accent-ink`(5.95) — `accent`(4.80)는 본문 하한 미달
- 활성 위치 표시에는 **쓰지 않는다.** 서류에서 위치를 가리키는 것은 색이 아니라
  2px 검정 괘선이다.

### `scrim` 이 별도 토큰인 이유

`base` 가 흰색이 된 뒤로 반투명 `base` 백드롭은 "흰 스크림 위의 흰 모달" 이 되어
경계가 사라진다. **타입체크·린트·빌드 어디에도 걸리지 않는 파손**이라 전용 토큰으로
분리했다. `token-audit` 이 옛 패턴을 금지 규칙으로 잡는다.

---

## §3 Typography

**Inter 400 / 600 / 800 — 정확히 3종.** 위계는 크기·자간·색으로 만든다.

`--font-display` 도 Inter 다. Montserrat 는 `weight:["500","600"]` 만 로드돼 상한이
600 이었고, 서류 조판의 표제는 800 을 쓴다. Inter 는 가변 폰트라 800 이 이미 로드돼
있어 추가 요청이 **0** 이다. 없는 웨이트를 지정하면 브라우저가 합성 볼드를 만들어
획이 뭉갠다.

| 웨이트 | 쓰는 곳 |
|---|---|
| 800 | H1 · 절 제목 |
| 600 | 표 헤더/행 라벨 · 폼 라벨 · 버튼 · 워드마크 |
| 400 | 본문 · 보조 · **모든 금액(mono)** |

- 금액·수치는 `--font-mono` + `tabular-nums`. **mono 에 800 을 쓰지 않는다** — 시스템
  mono 스택에 실 웨이트가 없어 합성된다. 합계는 웨이트가 아니라 **크기**로 키운다.
- 한글은 라틴보다 자폭이 넓다. 초대형 헤드라인은 `<lg` / `lg` clamp 를 분리해
  390px 오버플로를 막고 데스크톱 룩을 보존한다.
- 한글 줄바꿈: 어절 단위 유지 + 긴 토큰(이메일 등) 강제 분리.

---

## §4 선 위계 3단

전용 토큰을 만들지 않는다. 위계는 **색이 아니라 두께와 위치**가 만든다.

| 역할 | 구성 |
|---|---|
| 문서 경계 | 2px + `hi` — 표제 상하, 문서 종료, 머리말/판권면 경계 |
| 절 구획 | 1px + `hi` — 절 사이, 표 헤더 밑줄 |
| 행 구분 | 1px + `edge` — 표 행, 목록 행 |

---

## §5 Layout

- 콘텐츠 최대폭 **1200px**(셸) / 본문 컨테이너는 라우트별로 좁힌다(문서군 680px)
- 좌측 번호 열 **5.5rem** 고정 — 절 번호·표 행 번호·항목 번호가 같은 축에 선다
- 우측 정렬선 하나 — 요약 금액·행 금액·합계·스탯이 모두 같은 선에 떨어진다
- 표는 `table-fixed` + % 열 배분. **데스크톱 가로 스크롤 금지**
- 모바일에서 표를 `display:block` 스택으로 바꾸지 않는다 — AT 에서 표 역할이 사라진다.
  부가 열을 접는 방식을 쓴다
- 폭 검증: 1280 / 1366 / 1440 / 1600 / 1920 + 390. 여유 16px 미만의 "딱 맞음"은 실패

---

## §6 Depth

**없다.** 서류에 드롭섀도우는 이물이다. 깊이가 필요하면 면(`surface`)이나 괘선으로
표현한다. 예외는 툴팁 — 문서의 일부가 아니라 임시 오버레이이므로 **면을 반전**한다
(검정 면 + 흰 글씨).

---

## §7 Do's and Don'ts

**Do**
- 숫자는 mono + tabular-nums + 우측 정렬
- 컨트롤 경계는 `edge-strong`, 장식 괘선은 `edge`
- 상태는 색 + **도형/텍스트 병행** (채운 사각 / 빈 사각 / 가로줄)
- 포커스는 전역 `:focus-visible` outline 에 맡긴다
- 새 색을 추가하기 전에 `contrast-check` 를 돌린다

**Don't**
- 둥근 카드를 기본 단위로 쓰기
- 색만으로 의미 전달하기
- 컴포넌트마다 `ring-*` 포커스 링을 따로 만들기 — 중복이고, 알파 링은 흰 배경에서
  3:1 미달이라 오히려 접근성 후퇴다
- 반투명 sticky 셸 — 본문이 비쳐 면색이 스크롤에 따라 흔들린다
- 문서 파일에 클래스 예시를 적고 Tailwind 스캔에 노출하기

---

## §8 Responsive

- 390 / 768 / 1024 / 1280 / 1440 / 1920 에서 확인
- 모바일 하단 탭바는 **인증 후 화면에서만**. 공개 페이지에 앱 내비를 두면 거짓말이다
- 그리드 아이템에 `min-w-0` — 없으면 모바일 가로 오버플로
- `prefers-reduced-motion` 게이팅. 진입 `opacity:0` 잔존 금지

---

## §9 검증 도구

조판을 바꾸기 전에 이것부터 돌린다. 셋 다 **tsc·lint·build 가 잡지 못하는** 것을 잡는다.

```
npm test              # 계산 특성화 — 숫자가 움직이면 실패
npm run check:routes  # 라우트 스모크 (서버 필요)
npm run check:cssvar  # var(--x) 참조 ↔ 선언 대조. 미정의 = 색 무음 폴백
npm run check:tokens  # 옛 팔레트 잔재 (--stage=N / --all)
npm run check:contrast
```

자동화할 수 없는 것은 `.claude/plans/checklists/redesign-manual.md` 에 있다 —
스크림 반전, 모달 개폐, 인증 상태별 nav, 계산기 표시 금액.
